# Required for remote MCP deployment
MCP_AUTH_TOKEN=your-secret-token-here

# Transport mode: "stdio" (default) or "http" (remote deployment)
# MCP_TRANSPORT=http

# Optional overrides (defaults shown)
# MCP_HOST=0.0.0.0
# MCP_PORT=8001
# BRAIN3_API_URL=http://api:8000
